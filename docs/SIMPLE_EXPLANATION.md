# 📚 NEW-VOICE 2.0: Простое объяснение всех 5 фаз

**Дата:** 2026-01-17  
**Автор:** Для Дмитрия (владелец проекта)  
**Цель:** Понять что мы построили, простыми словами

---

## 🎯 Что такое NEW-VOICE 2.0?

Это платформа для создания голосовых ботов, которые могут:
- Звонить клиентам автоматически
- Разговаривать с ними по сценарию
- Собирать статистику звонков
- Управляться через веб-интерфейс

**Аналогия:** Представь call-центр, где вместо людей работают умные роботы. Ты можешь создать робота, дать ему сценарий разговора, список клиентов — и он начнёт звонить!

---

## 📖 5 Фаз разработки (что мы построили)

### Phase 1: База данных (Фундамент дома)

**Что это простыми словами:**
Мы создали "хранилище" для всех данных проекта. Как картотека в библиотеке, где каждая карточка — это информация.

**Что мы сделали:**
1. **Создали таблицы в PostgreSQL:**
   - `skillbases` — конфигурации ботов (как робот должен разговаривать)
   - `campaigns` — кампании обзвона (список задач для робота)
   - `call_tasks` — отдельные звонки (кому звонить, когда, статус)
   - `call_metrics` — статистика звонков (сколько длился, сколько стоил)
   - `call_logs` — детальные логи каждого разговора

2. **Использовали Alembic для миграций:**
   - Это как "история изменений" базы данных
   - Можно откатиться назад, если что-то пошло не так

**Зачем это нужно:**
Без базы данных негде хранить информацию о ботах, звонках, клиентах. Это фундамент всего проекта.

**Файлы:**
- `src/database/models.py` — описание таблиц
- `alembic/versions/001_*.py` — миграция для skillbases, campaigns, call_tasks
- `alembic/versions/002_*.py` — миграция для call_metrics, call_logs

---


### Phase 2: Skillbase Management (Мозг робота)

**Что это простыми словами:**
Мы научили систему понимать "как должен разговаривать бот". Это как написать инструкцию для сотрудника call-центра.

**Что мы сделали:**
1. **Создали Pydantic схемы для конфигурации:**
   - `ContextConfig` — роль бота (например, "администратор салона")
   - `FlowConfig` — сценарий разговора (шаги: приветствие → запись → прощание)
   - `VoiceConfig` — настройки голоса (какой голос, язык)
   - `LLMConfig` — настройки AI модели (какую использовать)

2. **Создали SkillbaseService:**
   - Создание новых ботов
   - Валидация конфигурации (проверка что всё правильно)
   - Обновление ботов с версионированием

3. **Интегрировали с VoiceAgent:**
   - Бот теперь загружает свою конфигурацию из базы данных
   - Может использовать разные сценарии для разных клиентов

**Зачем это нужно:**
Чтобы можно было создавать разных ботов для разных задач. Один бот для салона красоты, другой для доставки еды, третий для напоминаний о записи к врачу.

**Файлы:**
- `src/schemas/skillbase_schemas.py` — схемы конфигурации
- `src/services/skillbase_service.py` — логика управления ботами
- `src/voice_agent/skillbase_voice_agent.py` — голосовой агент с Skillbase

**Тестирование:**
- Создали тестового бота "Салон красоты"
- Проверили что конфигурация валидируется правильно
- Убедились что бот загружается из базы

---

### Phase 3: Deep Observability (Глаза и уши системы)

**Что это простыми словами:**
Мы добавили "счётчики" которые следят за всем что происходит во время звонка. Как фитнес-браслет следит за твоим здоровьем.

**Что мы сделали:**
1. **TelemetryService — сбор метрик:**
   - Сколько времени бот думал перед ответом
   - Сколько слов сказал клиент и бот
   - Сколько раз клиент перебил бота

2. **CostCalculator — подсчёт стоимости:**
   - Сколько стоило распознавание речи (STT)
   - Сколько стоила AI модель (LLM)
   - Сколько стоил синтез голоса (TTS)
   - Общая стоимость звонка

3. **QualityMetrics — оценка качества:**
   - Количество прерываний
   - Анализ тональности (клиент доволен или нет)
   - Классификация результата (успех, отказ, автоответчик)

**Зачем это нужно:**
Чтобы понимать:
- Насколько хорошо работает бот
- Сколько денег тратится на звонки
- Где можно улучшить качество

**Файлы:**
- `src/telemetry/telemetry_service.py` — сбор метрик
- `src/telemetry/cost_calculator.py` — подсчёт стоимости
- `src/telemetry/quality_metrics.py` — оценка качества
- `src/telemetry/metric_collector.py` — интеграция с VoiceAgent

**Тестирование:**
- Запустили тестовый звонок
- Проверили что все метрики собираются
- Убедились что стоимость считается правильно

---


### Phase 4: Campaign Management (Автоматический обзвон)

**Что это простыми словами:**
Мы создали "диспетчера", который управляет массовыми обзвонами. Как менеджер в call-центре раздаёт задачи операторам.

**Что мы сделали:**
1. **CampaignService — управление кампаниями:**
   - Создание кампании обзвона
   - Загрузка списка контактов из CSV/Excel
   - Запуск и остановка кампании
   - Rate limiting (не больше X звонков в минуту)

2. **CallTask — управление задачами:**
   - Каждый звонок = отдельная задача
   - Статусы: pending → in_progress → completed/failed
   - Автоматические повторы при ошибках
   - Планирование следующей попытки

3. **CampaignWorker — фоновый обработчик:**
   - Берёт задачи из очереди
   - Создаёт комнату в LiveKit
   - Звонит клиенту через SIP
   - Обновляет статус задачи

**Зачем это нужно:**
Чтобы можно было обзвонить 1000 клиентов автоматически, а не звонить каждому вручную.

**Файлы:**
- `src/services/campaign_service.py` — логика кампаний
- `src/workers/campaign_worker.py` — фоновый обработчик
- `scripts/test_campaign_worker.py` — тестовый скрипт

**Тестирование:**
- Создали тестовую кампанию с 3 контактами
- Запустили CampaignWorker
- Проверили что задачи обрабатываются
- Исправили баг с SQLAlchemy session state

**Найденные баги:**
- ❌ Ошибка: `rollback() can't be called here; commit() is already in progress`
- ✅ Исправление: Обернули `rollback()` в try-except

---

### Phase 5: API Layer (Веб-интерфейс для управления)

**Что это простыми словами:**
Мы создали "пульт управления" для всей системы. Как админ-панель сайта, где можно всё настроить через браузер.

**Что мы сделали:**
1. **Skillbase API (5 endpoints):**
   - GET /api/skillbases — список ботов
   - POST /api/skillbases — создать бота
   - GET /api/skillbases/{id} — получить бота
   - PUT /api/skillbases/{id} — обновить бота
   - DELETE /api/skillbases/{id} — удалить бота

2. **Campaign API (8 endpoints):**
   - GET /api/campaigns — список кампаний
   - POST /api/campaigns — создать кампанию
   - POST /api/campaigns/{id}/call-list — загрузить CSV с контактами
   - POST /api/campaigns/{id}/start — запустить обзвон
   - POST /api/campaigns/{id}/pause — остановить обзвон
   - GET /api/campaigns/{id} — получить кампанию
   - PUT /api/campaigns/{id} — обновить кампанию
   - DELETE /api/campaigns/{id} — удалить кампанию

3. **Analytics API (4 endpoints):**
   - GET /api/analytics/calls — история звонков
   - GET /api/analytics/calls/{id}/metrics — метрики звонка
   - GET /api/analytics/metrics — агрегированная статистика
   - WS /api/analytics/ws/calls/{id} — мониторинг в реальном времени

**Зачем это нужно:**
Чтобы управлять всем через веб-интерфейс, а не лезть в код или базу данных.

**Файлы:**
- `src/api/routers/skillbases.py` — API для ботов
- `src/api/routers/campaigns.py` — API для кампаний
- `src/api/routers/analytics.py` — API для аналитики
- `src/api/main.py` — главный файл FastAPI

**Тестирование и исправления:**

**Проблема 1: Неправильные импорты**
- ❌ Было: `from database.models import ...`
- ✅ Стало: `from src.database.models import ...`
- Исправлено в 4 файлах:
  - `skillbase_service.py`
  - `campaign_service.py`
  - `telemetry_service.py`
  - `campaign_worker.py`

**Проблема 2: Отсутствие библиотеки**
- ❌ Ошибка: `Form data requires "python-multipart" to be installed`
- ✅ Решение: `pip install python-multipart`

**Результат:**
- ✅ API сервер запущен на http://77.233.212.58:8000
- ✅ Swagger UI доступен на http://77.233.212.58:8000/docs
- ✅ Все 18 endpoints работают

---

