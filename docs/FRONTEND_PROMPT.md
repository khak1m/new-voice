# 🎨 Промт для создания фронтенда NEW-VOICE 2.0

## Задача
Создай современный веб-интерфейс (фронтенд) для платформы управления голосовыми ботами NEW-VOICE 2.0.

---

## 📋 О проекте

NEW-VOICE 2.0 — это платформа для создания и управления голосовыми ботами, которые автоматически звонят клиентам. Платформа имеет готовый REST API на FastAPI, к которому нужно подключить фронтенд.

**API Base URL:** `http://77.233.212.58:8000`  
**Swagger документация:** `http://77.233.212.58:8000/docs`

---

## 🎯 Требования к фронтенду

### Технологии
- **Framework:** React + TypeScript (или Next.js)
- **UI Library:** Tailwind CSS + shadcn/ui (или Material-UI)
- **State Management:** React Query для API запросов
- **Charts:** Recharts или Chart.js для графиков
- **Icons:** Lucide React или Heroicons
- **Routing:** React Router

### Язык интерфейса
- **ВАЖНО:** Весь интерфейс должен быть на РУССКОМ языке
- Все кнопки, надписи, меню, сообщения — на русском
- Названия полей форм — на русском
- Error messages и notifications — на русском
- Комментарии в коде могут быть на английском, но UI — только русский

### Дизайн
- Современный, чистый интерфейс
- Тёмная тема (опционально светлая)
- Responsive дизайн (работает на мобильных)
- Интуитивная навигация
- Красивые анимации и transitions

---

## 📱 Структура приложения

### 1. Главная страница (Dashboard)
**Путь:** `/`

**Что показывать:**
- **Карточки с метриками:**
  - Всего звонков (total_calls) + изменение за неделю (%)
  - Активных ботов (active_bots / total_bots)
  - Новых лидов (new_leads) + изменение за неделю (%)
  - Средняя длительность (avg_duration_min минут)

- **График звонков по дням** (последние 7 дней)
  - Линейный график с количеством звонков

- **Распределение результатов** (pie chart)
  - По outcomes: успех, отказ, автоответчик

- **Список последних 5 звонков** с кнопкой "Посмотреть все"

**API endpoints:**
```
GET /api/dashboard/stats?company_id=...
GET /api/calls?skip=0&limit=5
```

---

### 2. Управление ботами (Bots + Skillbases)
**Путь:** `/bots`

У вас есть ДВА типа ботов:
1. **Bots** (старая система) — `/api/bots`
2. **Skillbases** (новая система Enterprise) — `/api/skillbases`

Рекомендую сделать единый интерфейс с переключателем или вкладками.

#### 2.1 Список ботов
**Что показывать:**
- Таблица со всеми ботами:
  - Название (name)
  - Slug
  - Статус (активен/неактивен)
  - Опубликован (да/нет)
  - Версия
  - Дата создания
  - Действия: Редактировать, Удалить, Просмотр

**Функции:**
- Кнопка "Создать бота" (открывает форму)
- Фильтры: активные/неактивные, опубликованные
- Поиск по названию
- Пагинация

**API endpoints:**
```
# Skillbases (Enterprise)
GET /api/skillbases?skip=0&limit=20&is_active=true
POST /api/skillbases (создание)
DELETE /api/skillbases/{id} (удаление)

# Bots (старая система)
GET /api/bots?skip=0&limit=20&is_active=true
POST /api/bots (создание)
DELETE /api/bots/{id} (удаление)
```

#### 2.2 Создание/Редактирование бота
**Путь:** `/bots/new` или `/bots/{id}/edit`

**Форма с полями:**

**Основная информация:**
- Название (name) - текст
- Slug - текст (автогенерация из названия)
- Описание (description) - textarea
- Company ID - число
- Активен (is_active) - checkbox
- Опубликован (is_published) - checkbox

**Конфигурация (config):**

**Context (Контекст бота):**
- Роль (role) - textarea
  Пример: "Ты администратор салона красоты"
- Цель (goal) - textarea
  Пример: "Записать клиента на услугу"
- Правила (rules) - список строк (можно добавлять/удалять)
  Пример: ["Будь вежливым", "Говори кратко"]

**Flow (Сценарий разговора):**
- Приветствие (greeting) - textarea
- Шаги (steps) - список объектов:
  - Название шага
  - Описание
  - Вопрос к клиенту
  - Кнопки: Добавить шаг, Удалить шаг
- Прощание (closing) - textarea

**Voice (Настройки голоса):**
- Провайдер (provider) - select: "cartesia", "elevenlabs"
- ID голоса (voice_id) - текст
- Язык (language) - select: "ru", "en", "es"
- Скорость (speed) - slider (0.5 - 2.0)

**LLM (AI модель):**
- Провайдер (provider) - select: "groq", "openai"
- Модель (model) - select: "llama-3.3-70b-versatile", "gpt-4"
- Температура (temperature) - slider (0.0 - 1.0)
- Max tokens - число

**Tools (Инструменты):**
- Календарь (calendar) - checkbox
- Трансфер (transfer) - checkbox
- Номер для трансфера (transfer_number) - текст (если transfer включен)

**Кнопки:**
- Сохранить
- Отмена
- Предпросмотр JSON

**API endpoints:**
```
POST /api/skillbases (создание)
PUT /api/skillbases/{id} (обновление)
GET /api/skillbases/{id} (получение для редактирования)
```

---

### 3. Звонки (Calls)
**Путь:** `/calls`

#### 3.1 Список звонков
**Что показывать:**
- Таблица со всеми звонками:
  - ID звонка
  - Дата/время (started_at)
  - Телефон (caller_number / callee_number)
  - Бот (bot_id)
  - Направление (direction): входящий/исходящий
  - Длительность (duration_sec)
  - Результат (outcome)
  - Статус (status)
  - Действия: Детали

**Функции:**
- Фильтры: по боту, результату, статусу
- Поиск по телефону
- Пагинация

**API endpoints:**
```
GET /api/calls?skip=0&limit=20&bot_id=...&outcome=...&status=...
GET /api/calls/stats/summary?company_id=...
```

#### 3.2 Детали звонка
**Путь:** `/calls/{id}`

**Что показывать:**
- Информация о звонке
- Длительность, количество реплик (turn_count)
- Результат (outcome) и данные (outcome_data)
- Собранные данные (collected_data)
- **Лог разговора** (messages):
  - Кто сказал (role): user/assistant
  - Что сказал (content)
  - Время (created_at)

**API endpoints:**
```
GET /api/calls/{id}
GET /api/calls/{id}/messages
```

---

### 4. Лиды (Leads)
**Путь:** `/leads`

#### 4.1 Список лидов
**Что показывать:**
- Таблица с лидами:
  - Имя (name)
  - Телефон (phone)
  - Email (email)
  - Статус (status)
  - Бот (bot_id)
  - Дата создания
  - Заметки (notes)
  - Действия: Редактировать, Экспорт

**Функции:**
- Фильтры: по статусу, боту
- Поиск по имени/телефону
- Кнопка "Экспорт в CSV"
- Изменение статуса лида

**API endpoints:**
```
GET /api/leads?skip=0&limit=20&status=...&bot_id=...
PUT /api/leads/{id} (обновить статус/заметки)
GET /api/leads/stats/summary
GET /api/leads/export/csv (скачать CSV)
```

---

### 5. Базы знаний (Knowledge Bases)
**Путь:** `/knowledge-bases`

#### 5.1 Список баз знаний
**Что показывать:**
- Карточки с базами знаний:
  - Название (name)
  - Описание (description)
  - Количество документов (document_count)
  - Количество чанков (chunk_count)
  - Дата создания
  - Действия: Просмотр, Удалить

**Функции:**
- Кнопка "Создать базу знаний"
- Поиск по названию

**API endpoints:**
```
GET /api/knowledge-bases?company_id=...
POST /api/knowledge-bases (создание)
DELETE /api/knowledge-bases/{id}
```

#### 5.2 Детали базы знаний
**Путь:** `/knowledge-bases/{id}`

**Что показывать:**
- Информация о базе
- Список документов:
  - Название (title)
  - Тип (source_type)
  - Количество чанков (chunk_count)
  - Проиндексирован (is_indexed)
  - Дата добавления

**Функции:**
- Кнопка "Добавить документ"
- Форма поиска по базе знаний
- Результаты поиска с релевантностью (score)

**API endpoints:**
```
GET /api/knowledge-bases/{id}
GET /api/knowledge-bases/{id}/documents
POST /api/knowledge-bases/{id}/documents (добавить документ)
POST /api/knowledge-bases/{id}/search (поиск)
```

---

### 6. Управление кампаниями (Campaigns)
**Путь:** `/campaigns`

#### 6.1 Список кампаний
**Что показывать:**
- Таблица со всеми кампаниями:
  - Название
  - Бот (skillbase name)
  - Статус (draft, running, paused, completed)
  - Прогресс (completed_tasks / total_tasks)
  - Дата создания
  - Действия: Редактировать, Удалить, Запустить/Остановить

**Функции:**
- Кнопка "Создать кампанию"
- Фильтры по статусу
- Поиск по названию

**API endpoints:**
```
GET /api/campaigns?skip=0&limit=20
POST /api/campaigns/{id}/start (запуск)
POST /api/campaigns/{id}/pause (остановка)
DELETE /api/campaigns/{id}
```

#### 6.2 Создание/Редактирование кампании
**Путь:** `/campaigns/new` или `/campaigns/{id}/edit`

**Форма с полями:**

**Основная информация:**
- Название (name) - текст
- Описание (description) - textarea
- Company ID - число
- Выбор бота (skillbase_id) - select (список всех ботов)

**Настройки обзвона:**
- Звонков в минуту (calls_per_minute) - число (1-100)
- Время начала (daily_start_time) - time picker (HH:MM)
- Время окончания (daily_end_time) - time picker (HH:MM)
- Дни недели (days_of_week) - checkboxes (пн-вс)

**Retry логика:**
- Максимум попыток (max_retry_attempts) - число (0-10)
- Задержка между попытками (retry_delay_minutes) - число (5-1440)

**Загрузка контактов:**
- Drag & Drop зона для CSV/Excel файла
- Или кнопка "Выбрать файл"
- Показать пример формата CSV:
  ```
  phone_number,name
  +79991234567,Иван Иванов
  +79997654321,Мария Петрова
  ```
- После загрузки показать результат:
  - Всего строк
  - Успешно загружено
  - Ошибки (если есть)

**Кнопки:**
- Сохранить
- Отмена

**API endpoints:**
```
POST /api/campaigns (создание)
PUT /api/campaigns/{id} (обновление)
POST /api/campaigns/{id}/call-list (загрузка CSV)
GET /api/campaigns/{id}
```

#### 6.3 Детали кампании
**Путь:** `/campaigns/{id}`

**Что показывать:**
- Информация о кампании
- Статистика:
  - Всего задач
  - Выполнено
  - В процессе
  - Ожидают
  - Провалено
- Прогресс бар
- Кнопки: Запустить/Остановить, Редактировать, Удалить
- Таблица задач (CallTasks):
  - Телефон
  - Имя
  - Статус
  - Попытка
  - Дата последней попытки
  - Результат

**API endpoints:**
```
GET /api/campaigns/{id}
GET /api/analytics/calls?campaign_id={id}
```

---

### 7. Аналитика звонков (Analytics)
**Путь:** `/analytics`

#### 7.1 История звонков
**Что показывать:**
- Фильтры:
  - Период (date range picker)
  - Бот (select)
  - Кампания (select)
  - Результат (select): все, успех, отказ, автоответчик
  - Статус (select): все, completed, failed

- Таблица звонков:
  - ID звонка
  - Дата/время
  - Телефон
  - Бот
  - Кампания
  - Длительность
  - Результат
  - Стоимость
  - Действия: Детали

**API endpoints:**
```
GET /api/analytics/calls?page=1&page_size=20&start_date=...&end_date=...
```

#### 7.2 Детали звонка
**Путь:** `/analytics/calls/{id}`

**Что показывать:**

**Основная информация:**
- ID звонка
- Дата/время начала и окончания
- Длительность
- Телефон
- Бот
- Кампания
- Результат (outcome)
- Статус

**Метрики производительности:**
- Среднее время ответа бота (avg_eou_latency)
- Минимальное время ответа
- Максимальное время ответа
- Количество реплик (turn_count)

**Метрики использования:**
- Токены LLM (llm_tokens_total)
- Символы TTS (tts_characters_total)
- Длительность STT (stt_duration_seconds)

**Стоимость (breakdown):**
- STT: $X.XX
- LLM: $X.XX
- TTS: $X.XX
- LiveKit: $X.XX
- **Итого:** $X.XX

**Качество:**
- Прерывания (interruptions_count)
- Тональность (sentiment_score)
- Уверенность в результате (outcome_confidence)

**API endpoints:**
```
GET /api/analytics/calls/{id}/metrics
```

#### 7.3 Агрегированная статистика
**Путь:** `/analytics/metrics`

**Что показывать:**
- Фильтры: период, бот, кампания
- Карточки с метриками:
  - Всего звонков
  - Успешных
  - Провалено
  - Средняя длительность
  - Средняя стоимость
  - Общая стоимость
- Графики:
  - Звонки по дням (линейный)
  - Распределение результатов (pie chart)
  - Средняя стоимость по дням (bar chart)
  - Время ответа бота (histogram)

**API endpoints:**
```
GET /api/analytics/metrics?start_date=...&end_date=...
```

---

### 8. Real-time мониторинг (опционально)
**Путь:** `/monitoring/{call_id}`

**Что показывать:**
- Статус звонка в реальном времени
- Лог реплик (кто что сказал)
- Текущие метрики
- Обновление через WebSocket

**WebSocket endpoint:**
```
WS /api/analytics/ws/calls/{id}
```

**Типы сообщений:**
```json
{
  "type": "init",
  "data": { "call_id": "...", "status": "..." }
}
{
  "type": "turn",
  "data": { "speaker": "bot", "text": "Здравствуйте!" }
}
{
  "type": "metrics",
  "data": { "latency": 0.5, "cost": 0.01 }
}
```

---

## 📊 Полный список API Endpoints

### Dashboard
- `GET /api/dashboard/stats` — статистика для главной страницы

### Боты (два типа)
**Skillbases (Enterprise):**
- `GET /api/skillbases` — список
- `POST /api/skillbases` — создать
- `GET /api/skillbases/{id}` — получить
- `PUT /api/skillbases/{id}` — обновить
- `DELETE /api/skillbases/{id}` — удалить

**Bots (старая система):**
- `GET /api/bots` — список
- `POST /api/bots` — создать
- `GET /api/bots/{id}` — получить
- `PUT /api/bots/{id}` — обновить
- `DELETE /api/bots/{id}` — удалить

### Звонки
- `GET /api/calls` — список звонков
- `GET /api/calls/{id}` — детали звонка
- `GET /api/calls/{id}/messages` — сообщения звонка
- `GET /api/calls/stats/summary` — статистика

### Лиды
- `GET /api/leads` — список лидов
- `GET /api/leads/{id}` — получить лид
- `PUT /api/leads/{id}` — обновить лид
- `GET /api/leads/stats/summary` — статистика
- `GET /api/leads/export/csv` — экспорт в CSV

### Базы знаний
- `GET /api/knowledge-bases` — список
- `POST /api/knowledge-bases` — создать
- `GET /api/knowledge-bases/{id}` — получить
- `DELETE /api/knowledge-bases/{id}` — удалить
- `GET /api/knowledge-bases/{id}/documents` — список документов
- `POST /api/knowledge-bases/{id}/documents` — добавить документ
- `POST /api/knowledge-bases/{id}/search` — поиск

### Кампании
- `GET /api/campaigns` — список
- `POST /api/campaigns` — создать
- `GET /api/campaigns/{id}` — получить
- `PUT /api/campaigns/{id}` — обновить
- `DELETE /api/campaigns/{id}` — удалить
- `POST /api/campaigns/{id}/call-list` — загрузить CSV
- `POST /api/campaigns/{id}/start` — запустить
- `POST /api/campaigns/{id}/pause` — остановить

### Аналитика
- `GET /api/analytics/calls` — история с фильтрами
- `GET /api/analytics/calls/{id}/metrics` — детальные метрики
- `GET /api/analytics/metrics` — агрегированные метрики
- `WS /api/analytics/ws/calls/{id}` — WebSocket мониторинг

**Всего:** ~40+ endpoints

---

## 🎨 UI/UX рекомендации

### Цветовая схема
- **Primary:** Синий (#3B82F6)
- **Success:** Зелёный (#10B981)
- **Warning:** Жёлтый (#F59E0B)
- **Error:** Красный (#EF4444)
- **Background:** Тёмно-серый (#1F2937) или белый
- **Text:** Белый или тёмно-серый

### Компоненты
- **Кнопки:** Rounded, с иконками, hover эффекты, текст на русском
- **Таблицы:** Сортировка, пагинация, hover на строках, заголовки на русском
- **Формы:** Валидация, error messages на русском, loading states
- **Карточки:** Shadow, rounded corners
- **Модальные окна:** Для подтверждения удаления, текст на русском
- **Toast notifications:** Для успеха/ошибок, сообщения на русском

### Статусы (цветовое кодирование)
- **draft:** Серый
- **running:** Зелёный (пульсирующий)
- **paused:** Жёлтый
- **completed:** Синий
- **failed:** Красный

### Иконки
- Dashboard: 📊 (Панель управления)
- Боты: 🤖 (Боты)
- Звонки: 📞 (Звонки)
- Лиды: 👥 (Лиды)
- Базы знаний: 📚 (Базы знаний)
- Кампании: 📣 (Кампании)
- Аналитика: 📈 (Аналитика)
- Настройки: ⚙️ (Настройки)

### Примеры русских надписей
- Кнопки: "Создать", "Сохранить", "Отмена", "Удалить", "Редактировать"
- Статусы: "Активен", "Неактивен", "Запущена", "На паузе", "Завершена"
- Сообщения: "Успешно сохранено", "Произошла ошибка", "Загрузка..."
- Формы: "Название", "Описание", "Телефон", "Дата создания"
- Таблицы: "Всего записей", "Показано", "Страница", "из"

---

## 📦 Структура проекта

```
frontend/
├── src/
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Sidebar.tsx
│   │   │   ├── Header.tsx
│   │   │   └── Layout.tsx
│   │   ├── bots/
│   │   │   ├── BotList.tsx
│   │   │   ├── BotForm.tsx
│   │   │   └── BotCard.tsx
│   │   ├── calls/
│   │   │   ├── CallList.tsx
│   │   │   ├── CallDetails.tsx
│   │   │   └── CallMessages.tsx
│   │   ├── leads/
│   │   │   ├── LeadList.tsx
│   │   │   ├── LeadForm.tsx
│   │   │   └── LeadExport.tsx
│   │   ├── knowledge/
│   │   │   ├── KnowledgeBaseList.tsx
│   │   │   ├── DocumentList.tsx
│   │   │   └── SearchForm.tsx
│   │   ├── campaigns/
│   │   │   ├── CampaignList.tsx
│   │   │   ├── CampaignForm.tsx
│   │   │   ├── CampaignDetails.tsx
│   │   │   └── FileUpload.tsx
│   │   ├── analytics/
│   │   │   ├── CallsList.tsx
│   │   │   ├── CallDetails.tsx
│   │   │   ├── MetricsCards.tsx
│   │   │   └── Charts.tsx
│   │   └── ui/
│   │       ├── Button.tsx
│   │       ├── Input.tsx
│   │       ├── Select.tsx
│   │       ├── Table.tsx
│   │       └── Modal.tsx
│   ├── pages/
│   │   ├── Dashboard.tsx
│   │   ├── Bots.tsx
│   │   ├── Calls.tsx
│   │   ├── Leads.tsx
│   │   ├── KnowledgeBases.tsx
│   │   ├── Campaigns.tsx
│   │   └── Analytics.tsx
│   ├── api/
│   │   ├── client.ts (axios instance)
│   │   ├── bots.ts
│   │   ├── calls.ts
│   │   ├── leads.ts
│   │   ├── knowledge.ts
│   │   ├── campaigns.ts
│   │   └── analytics.ts
│   ├── types/
│   │   ├── bot.ts
│   │   ├── call.ts
│   │   ├── lead.ts
│   │   ├── knowledge.ts
│   │   ├── campaign.ts
│   │   └── analytics.ts
│   ├── hooks/
│   │   ├── useBots.ts
│   │   ├── useCalls.ts
│   │   ├── useLeads.ts
│   │   ├── useKnowledge.ts
│   │   ├── useCampaigns.ts
│   │   └── useAnalytics.ts
│   ├── utils/
│   │   ├── formatters.ts
│   │   └── validators.ts
│   └── App.tsx
├── package.json
└── tsconfig.json
```

---

## 🔧 API клиент (пример)

```typescript
// src/api/client.ts
import axios from 'axios';

const API_BASE_URL = 'http://77.233.212.58:8000';

export const apiClient = axios.create({
  baseURL: API_BASE_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// src/api/bots.ts
export const botsApi = {
  getAll: (params?: { skip?: number; limit?: number }) =>
    apiClient.get('/api/bots', { params }),
  
  getById: (id: string) =>
    apiClient.get(`/api/bots/${id}`),
  
  create: (data: BotCreate) =>
    apiClient.post('/api/bots', data),
  
  update: (id: string, data: BotUpdate) =>
    apiClient.put(`/api/bots/${id}`, data),
  
  delete: (id: string) =>
    apiClient.delete(`/api/bots/${id}`),
};

// src/api/skillbases.ts
export const skillbasesApi = {
  getAll: (params?: { skip?: number; limit?: number }) =>
    apiClient.get('/api/skillbases', { params }),
  
  getById: (id: number) =>
    apiClient.get(`/api/skillbases/${id}`),
  
  create: (data: SkillbaseCreate) =>
    apiClient.post('/api/skillbases', data),
  
  update: (id: number, data: SkillbaseUpdate) =>
    apiClient.put(`/api/skillbases/${id}`, data),
  
  delete: (id: number) =>
    apiClient.delete(`/api/skillbases/${id}`),
};
```

---

## 📝 TypeScript типы (пример)

```typescript
// src/types/bot.ts
export interface Bot {
  id: string;
  company_id: string;
  name: string;
  slug: string;
  description?: string;
  scenario_config: Record<string, any>;
  voice_config: VoiceConfig;
  llm_config: LLMConfig;
  is_active: boolean;
  is_published: boolean;
  created_at: string;
  updated_at: string;
}

export interface VoiceConfig {
  tts_provider: 'cartesia' | 'elevenlabs';
  tts_voice_id: string;
  stt_provider: 'deepgram';
  stt_language: string;
}

export interface LLMConfig {
  provider: 'groq' | 'openai' | 'ollama';
  model: string;
  temperature: number;
}

// src/types/call.ts
export interface Call {
  id: string;
  bot_id?: string;
  company_id?: string;
  direction: 'inbound' | 'outbound';
  caller_number?: string;
  callee_number?: string;
  outcome?: string;
  outcome_data: Record<string, any>;
  collected_data: Record<string, any>;
  duration_sec: number;
  turn_count: number;
  language: string;
  status: string;
  started_at: string;
  ended_at?: string;
}

export interface Message {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  state_id?: string;
  created_at: string;
}

// src/types/lead.ts
export interface Lead {
  id: string;
  call_id?: string;
  bot_id?: string;
  company_id?: string;
  name?: string;
  phone?: string;
  email?: string;
  data: Record<string, any>;
  status: string;
  notes?: string;
  webhook_sent: boolean;
  created_at: string;
  updated_at: string;
}
```

---

## ✅ Чек-лист функциональности

### Обязательные функции:
- [ ] Dashboard с общей статистикой
- [ ] Список ботов (Bots + Skillbases) с фильтрами
- [ ] Создание/редактирование бота (полная форма)
- [ ] Удаление бота с подтверждением
- [ ] Список звонков с фильтрами
- [ ] Детали звонка с логом разговора
- [ ] Список лидов с фильтрами
- [ ] Обновление статуса лида
- [ ] Экспорт лидов в CSV
- [ ] Список баз знаний
- [ ] Добавление документов в базу знаний
- [ ] Поиск по базе знаний
- [ ] Список кампаний с фильтрами
- [ ] Создание/редактирование кампании
- [ ] Загрузка CSV с контактами
- [ ] Запуск/остановка кампании
- [ ] Детали кампании с прогрессом
- [ ] История звонков с фильтрами (Analytics)
- [ ] Детали звонка с метриками (Analytics)
- [ ] Агрегированная статистика с графиками
- [ ] Responsive дизайн
- [ ] Error handling и loading states
- [ ] Toast notifications
- [ ] Весь интерфейс на русском языке

### Опциональные функции:
- [ ] Real-time мониторинг через WebSocket
- [ ] Экспорт данных в CSV
- [ ] Тёмная/светлая тема
- [ ] Поиск по всем таблицам
- [ ] Сортировка таблиц
- [ ] Сохранение фильтров в URL
- [ ] Keyboard shortcuts

---

## 🚀 Инструкции по запуску

После создания фронтенда:

1. Установить зависимости:
```bash
npm install
```

2. Создать `.env` файл:
```
VITE_API_BASE_URL=http://77.233.212.58:8000
```

3. Запустить dev сервер:
```bash
npm run dev
```

4. Собрать для production:
```bash
npm run build
```

---

## 📚 Дополнительная информация

**Swagger документация API:** http://77.233.212.58:8000/docs

Там можно посмотреть:
- Все доступные endpoints
- Схемы request/response
- Попробовать API прямо в браузере

**Важно:**
- Все API endpoints асинхронные
- Используй React Query для кэширования
- Добавь loading states везде
- Обрабатывай ошибки (400, 404, 500)
- Валидируй формы перед отправкой
- **ВСЕ тексты в интерфейсе должны быть на РУССКОМ языке**

---

## 🌐 Локализация (i18n)

Хотя интерфейс должен быть на русском, рекомендуется использовать i18n библиотеку (react-i18next) для будущей поддержки других языков:

```typescript
// Пример структуры переводов
const translations = {
  ru: {
    common: {
      save: "Сохранить",
      cancel: "Отмена",
      delete: "Удалить",
      edit: "Редактировать",
      create: "Создать",
      loading: "Загрузка...",
      error: "Произошла ошибка",
      success: "Успешно выполнено"
    },
    dashboard: {
      title: "Панель управления",
      totalCalls: "Всего звонков",
      successfulCalls: "Успешных звонков",
      averageDuration: "Средняя длительность",
      totalCost: "Общая стоимость"
    },
    bots: {
      title: "Боты",
      createBot: "Создать бота",
      editBot: "Редактировать бота",
      deleteBot: "Удалить бота",
      botName: "Название бота",
      botDescription: "Описание"
    },
    calls: {
      title: "Звонки",
      callHistory: "История звонков",
      callDetails: "Детали звонка",
      duration: "Длительность",
      outcome: "Результат"
    },
    leads: {
      title: "Лиды",
      newLead: "Новый лид",
      status: "Статус",
      exportCsv: "Экспорт в CSV"
    },
    knowledge: {
      title: "Базы знаний",
      createKb: "Создать базу знаний",
      addDocument: "Добавить документ",
      search: "Поиск"
    },
    campaigns: {
      title: "Кампании",
      createCampaign: "Создать кампанию",
      startCampaign: "Запустить кампанию",
      pauseCampaign: "Остановить кампанию",
      uploadContacts: "Загрузить контакты"
    },
    analytics: {
      title: "Аналитика",
      callHistory: "История звонков",
      metrics: "Метрики",
      callDetails: "Детали звонка"
    }
  }
};
```

---

## 🎯 Итоговый результат

Должен получиться современный, функциональный веб-интерфейс для управления голосовыми ботами, где можно:

1. **Управлять ботами:**
   - Создавать и настраивать ботов (Bots и Skillbases)
   - Настраивать голос, AI модель, сценарии
   - Активировать/деактивировать

2. **Работать со звонками:**
   - Просматривать историю звонков
   - Читать логи разговоров
   - Анализировать результаты

3. **Управлять лидами:**
   - Просматривать собранные лиды
   - Изменять статусы
   - Экспортировать в CSV

4. **Работать с базами знаний:**
   - Создавать базы знаний
   - Добавлять документы
   - Искать информацию

5. **Запускать кампании обзвона:**
   - Создавать кампании
   - Загружать списки контактов
   - Мониторить прогресс в реальном времени

6. **Анализировать результаты:**
   - Смотреть детальную статистику
   - Видеть графики и метрики
   - Считать стоимость звонков

Всё должно быть интуитивно понятно, красиво, на русском языке и работать быстро! 🚀
