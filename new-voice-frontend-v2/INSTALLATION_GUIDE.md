# 📦 Installation Guide - NEW-VOICE 2.0 Frontend

**Дата:** 26 января 2026  
**Версия:** 2.0.0

---

## 🔧 Предварительные требования

### 1. Node.js
**Требуется:** Node.js >= 18.0.0

Проверить версию:
```bash
node --version
```

Если Node.js не установлен или версия < 18, скачайте с [nodejs.org](https://nodejs.org/)

---

### 2. pnpm
**Требуется:** pnpm >= 8.0.0

#### Установка pnpm (Windows)

**Вариант 1: Через npm (рекомендуется)**
```bash
npm install -g pnpm
```

**Вариант 2: Через PowerShell**
```powershell
iwr https://get.pnpm.io/install.ps1 -useb | iex
```

**Вариант 3: Через Chocolatey**
```bash
choco install pnpm
```

#### Проверка установки
```bash
pnpm --version
```

Должно вывести версию >= 8.0.0

---

## 📥 Установка зависимостей

### 1. Перейти в директорию проекта
```bash
cd new-voice-frontend-v2
```

### 2. Установить зависимости
```bash
pnpm install
```

Эта команда установит все зависимости для всех пакетов в monorepo.

**Ожидаемый результат:**
```
Packages: +XXX
Progress: resolved XXX, reused XXX, downloaded XX, added XXX, done
```

---

## ✅ Проверка установки

### 1. Проверить TypeScript типы
```bash
pnpm type-check
```

**Ожидаемый результат:** Нет ошибок компиляции

### 2. Запустить линтер
```bash
pnpm lint
```

**Ожидаемый результат:** Нет ошибок линтинга (или только warnings)

### 3. Форматировать код
```bash
pnpm format
```

**Ожидаемый результат:** Код отформатирован

---

## 📦 Структура пакетов

После установки зависимостей структура будет:

```
new-voice-frontend-v2/
├── node_modules/          # Root dependencies
├── packages/
│   ├── types/
│   │   └── node_modules/  # Package dependencies
│   ├── shared/
│   │   └── node_modules/
│   ├── api-client/
│   │   └── node_modules/
│   └── ui/
│       └── node_modules/
└── pnpm-lock.yaml         # Lock file
```

---

## 🔍 Проверка отдельных пакетов

### Проверить @new-voice/types
```bash
pnpm --filter @new-voice/types type-check
```

### Проверить @new-voice/shared
```bash
pnpm --filter @new-voice/shared type-check
```

### Проверить @new-voice/api-client
```bash
pnpm --filter @new-voice/api-client type-check
```

### Проверить @new-voice/ui
```bash
pnpm --filter @new-voice/ui type-check
```

---

## 🐛 Troubleshooting

### Проблема: pnpm не найден после установки

**Решение 1:** Перезапустить терминал

**Решение 2:** Добавить pnpm в PATH вручную
```bash
# Найти путь к pnpm
npm config get prefix

# Добавить в PATH (Windows)
# Обычно: C:\Users\<Username>\AppData\Roaming\npm
```

---

### Проблема: Ошибки при установке зависимостей

**Решение 1:** Очистить кэш
```bash
pnpm store prune
pnpm install
```

**Решение 2:** Удалить node_modules и lock file
```bash
# Windows PowerShell
Remove-Item -Recurse -Force node_modules, pnpm-lock.yaml
pnpm install
```

---

### Проблема: TypeScript ошибки

**Решение:** Проверить версию TypeScript
```bash
pnpm list typescript
```

Должна быть версия 5.3.3

---

### Проблема: Workspace dependencies не найдены

**Решение:** Убедиться что pnpm-workspace.yaml корректен
```yaml
packages:
  - 'packages/*'
  - 'modules/*'
  - 'src'
```

---

## 📝 Полезные команды

### Установка новой зависимости

**В root:**
```bash
pnpm add -D <package-name>
```

**В конкретный пакет:**
```bash
pnpm --filter @new-voice/ui add <package-name>
```

**Workspace dependency:**
```bash
pnpm --filter @new-voice/ui add @new-voice/shared@workspace:*
```

---

### Обновление зависимостей

**Проверить устаревшие:**
```bash
pnpm outdated
```

**Обновить все:**
```bash
pnpm update
```

**Обновить конкретный пакет:**
```bash
pnpm update <package-name>
```

---

### Удаление зависимости

**Из root:**
```bash
pnpm remove <package-name>
```

**Из конкретного пакета:**
```bash
pnpm --filter @new-voice/ui remove <package-name>
```

---

## ✅ Checklist установки

- [ ] Node.js >= 18.0.0 установлен
- [ ] pnpm >= 8.0.0 установлен
- [ ] `pnpm install` выполнен успешно
- [ ] `pnpm type-check` проходит без ошибок
- [ ] `pnpm lint` проходит без критичных ошибок
- [ ] Все пакеты имеют node_modules
- [ ] pnpm-lock.yaml создан

---

## 🚀 Следующие шаги

После успешной установки:

1. ✅ Проверить что всё работает: `pnpm type-check`
2. ✅ Изучить структуру проекта: `README.md`
3. ✅ Начать разработку Phase 4: Модуль Skillbases

---

**Автор:** AI Architect  
**Дата:** 26 января 2026

