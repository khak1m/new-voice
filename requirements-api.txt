# NEW-VOICE 2.0 - API-only dependencies
#
# This file is used for deploying the FastAPI backend without the voice agent
# stack (LiveKit Agents). It avoids dependency conflicts on servers where the
# agent runtime isn't required.
#
# Install:
#   pip install -r requirements-api.txt

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# LLM Providers (optional but kept)
groq==0.4.0
openai==1.12.0

# Database
sqlalchemy[asyncio]==2.0.25
psycopg2-binary==2.9.9
asyncpg==0.29.0
alembic==1.13.1

# Redis
redis==5.0.1
aioredis==2.0.1

# Qdrant
qdrant-client==1.7.0

# RAG / Embeddings
sentence-transformers==2.3.1

# Utils
python-dotenv==1.0.0
pydantic==2.6.0
pydantic-settings==2.1.0
httpx==0.26.0
aiofiles==23.2.1
pyyaml==6.0.1
pandas==2.2.0
openpyxl==3.1.2

# Audio processing
numpy==1.26.0
soundfile==0.12.1
