# 📝 Шпаргалка - Деплой на Сервер

## 🚀 Быстрый Деплой (3 команды)

### На вашем компьютере:

```bash
cd new-voice-frontend
npm run build
deploy.bat
```

### На сервере:

```bash
ssh root@77.233.212.58
cd /root/new-voice/src/api
cp main_production.py main.py
pkill -f uvicorn
cd /root/new-voice
nohup python -m uvicorn src.api.main:app --host 0.0.0.0 --port 8000 > logs/api.log 2>&1 &
```

### Откройте:

```
http://77.233.212.58:8000
```

**Готово!** 🎉

---

## 📋 Команды для Сервера

### Управление API

```bash
# Запустить
cd /root/new-voice
nohup python -m uvicorn src.api.main:app --host 0.0.0.0 --port 8000 > logs/api.log 2>&1 &

# Остановить
pkill -f uvicorn

# Перезапустить
pkill -f uvicorn && cd /root/new-voice && nohup python -m uvicorn src.api.main:app --host 0.0.0.0 --port 8000 > logs/api.log 2>&1 &

# Логи
tail -f /root/new-voice/logs/api.log

# Статус
ps aux | grep uvicorn
```

### Systemd (автозапуск)

```bash
# Запустить
systemctl start new-voice

# Остановить
systemctl stop new-voice

# Перезапустить
systemctl restart new-voice

# Статус
systemctl status new-voice

# Логи
journalctl -u new-voice -f
```

---

## 🔄 Обновление Фронтенда

```bash
# 1. Соберите
cd new-voice-frontend
npm run build

# 2. Загрузите
deploy.bat

# Готово! Перезапуск НЕ нужен
```

---

## 🔍 Проверка

```bash
# API работает?
curl http://77.233.212.58:8000/health

# Фронтенд загружен?
ssh root@77.233.212.58 "ls -la /root/new-voice/frontend-dist/"

# Процесс запущен?
ssh root@77.233.212.58 "ps aux | grep uvicorn"

# Порт открыт?
ssh root@77.233.212.58 "netstat -tulpn | grep 8000"
```

---

## 🐛 Быстрые Фиксы

### Фронтенд не загружается

```bash
ssh root@77.233.212.58
chmod -R 755 /root/new-voice/frontend-dist/
```

### API не отвечает

```bash
ssh root@77.233.212.58
cd /root/new-voice
pkill -f uvicorn
nohup python -m uvicorn src.api.main:app --host 0.0.0.0 --port 8000 > logs/api.log 2>&1 &
```

### Изменения не видны

```
Ctrl+Shift+R в браузере (очистить кэш)
```

---

## 📂 Структура на Сервере

```
/root/new-voice/
├── frontend-dist/          # Собранный фронтенд
│   ├── index.html
│   └── assets/
├── src/
│   └── api/
│       ├── main.py         # Продакшен версия
│       └── main_dev.py     # Оригинал
├── logs/
│   └── api.log            # Логи API
└── .env                   # Конфигурация
```

---

## 🔗 Полезные Ссылки

- **Фронтенд:** http://77.233.212.58:8000
- **API Docs:** http://77.233.212.58:8000/docs
- **Health Check:** http://77.233.212.58:8000/health

---

**Дата:** 2026-01-18
